{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "rm -f public/hot && npm install --include=dev --ignore-scripts && npm run build && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "preDeployCommand": "sh ./railway/init-app.sh",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300
  }
}